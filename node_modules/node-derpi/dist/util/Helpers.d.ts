/**
 * Slugifies a URL parameter in the same way Derpibooru does, for fetching tags
 *
 * @private
 * @export
 * @param {string} param The parameter to slugify
 */
export declare function slugify(param: string): string;
